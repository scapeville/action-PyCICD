name: PyCICD
description: foo
author: Nicholas Valentinus
branding:
  icon: tool
  color: orange

inputs:
  foo:
    description: 
    required: true

runs:
  using: 'composite'
  steps:
    - uses: ./.github/workflows/core.yml