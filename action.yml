name: PyCICD
description: foo
author: Nicholas Valentinus
branding:
  icon: tool
  color: orange

inputs:
  foo:
    description: 
    required: true

runs:
  using: 'composite'
  steps:
    - uses: python $GITHUB_ACTION_PATH/engine/check_commit_msg.py